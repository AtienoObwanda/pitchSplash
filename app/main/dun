# @main.route('/react/<int:pitch_id>',methods = ['POST','GET'])
# @login_required
# def react(pitch_id):
#     pitchlike = Like.get_like(pitch_id)
#     newlike = Like(user_id =current_user.id,pitch_id = pitch_id )
#     pitch = Pitch.query.get_or_404(pitch_id)








#Like
# class Like(db.Model):
#     id = db.Column(db.Integer, primary_key=True)
#     user_id = db.Column(db.Integer,db.ForeignKey('user.id'),nullable=False) #Id of the user
#     pitch_id = db.Column(db.Integer, db.ForeignKey('pitch.id'),nullable=False)

# def __repr__(self):
#         return f'{self.user_id}:{self.pitch_id}'


# Dislike
# class Dislike(db.Model):
#     id = db.Column(db.Integer, primary_key=True)
#     user_id = db.Column(db.Integer,db.ForeignKey('user.id'),nullable=False) #Id of the user
#     pitch_id = db.Column(db.Integer, db.ForeignKey('pitch.id'),nullable=False)

# def __repr__(self):
#         return f'{self.user_id}:{self.pitch_id}'